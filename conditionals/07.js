let orderSize = "Medium";
let extraShot = true;
let coffee;

if (extraShot) {
    coffee = orderSize + " coffee with an extra shot";
} else {
    coffee = orderSize + " coffee";
}

console.log("Order: ", coffee);
